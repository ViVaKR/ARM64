# 🗺️ 훈민정음 OS "훈" - 맥OS vs 리눅스 AArch64 마스터 시스템 콜 지도

> [!IMPORTANT]
> **시스템 콜(Syscall)은 OS 커널의 입구 번호표라네!**  
> 하드웨어(AArch64) 레지스터는 같아도, 맥 OS(Mach/BSD)와 리눅스(POSIX/Linux) 커널이 나눠주는 번호표는 완전히 다르므로, 본 지도를 보고 정확한 번호에 저격 사격을 가해야 한다네!

---

## 📌 시스템 콜 호출 시 레지스터 약속 (Calling Convention)

### 🍎 맥 OS (macOS AArch64)
* **시스템 콜 번호 탑승**: `x16` 레지스터
* **인자 배달**: `x0` (1번째), `x1` (2번째), `x2` (3번째), `x3` (4번째), `x4` (5번째), `x5` (6번째)
* **결과 수신**: 성공 시 `x0`에 결과값 반환 / 에러 발생 시 Carry Flag(`C`) 세팅 및 `x0`에 에러코드 반환.

### 🐧 리눅스 (AArch64 Linux)
* **시스템 콜 번호 탑승**: `x8` 레지스터
* **인자 배달**: `x0` (1번째), `x1` (2번째), `x2` (3번째), `x3` (4번째), `x4` (5번째), `x5` (6번째)
* **결과 수신**: 성공 시 `x0`에 결과값 반환 / 에러 발생 시 `x0`에 음수값(`-에러코드`) 반환.

---

## 📊 핵심 시스템 콜 대조표 (OS 개발 필수 40종)

| 번호 | 한글 기능명 | 용도 및 설명 | **맥 OS (x16)** | **리눅스 (x8)** |
| :---: | :--- | :--- | :---: | :---: |
| **1** | **퇴근종료** (exit) | 프로세스를 안전하게 종료하고 부모에게 상태 전달 | **`1`** | **`93`** |
| **2** | **화면출력** (write) | 파일 디스크립터(1 = 화면)에 바이트 스트림 쓰기 | **`4`** | **`64`** |
| **3** | **입력받기** (read) | 파일 디스크립터(0 = 키보드)로부터 데이터 읽기 | **`3`** | **`63`** |
| **4** | **파일열기** (open) | 경로에 있는 파일을 읽기/쓰기 모드로 열기 | **`5`** | **`56`** |
| **5** | **파일닫기** (close) | 열려 있는 파일 핸들을 닫고 메모리 반환 | **`6`** | **`57`** |
| **6** | **동적확장** (brk) | 힙(Heap) 영역의 끝 주소를 늘리거나 줄이기 (malloc 원형) | **`지원안함`** | **`214`** |
| **7** | **기억할당** (mmap) | 새 가상 메모리 영역을 물리 페이지에 매핑 | **`197`** | **`222`** |
| **8** | **기억해제** (munmap) | 할당된 가상 메모리 영역을 회수하고 맵 해제 | **`73`** | **`215`** |
| **9** | **자식분신** (fork/clone) | 현재 실행 중인 프로세스와 똑같은 복제 프로세스 생성 | **`2`** | **`220`** |
| **10** | **새판짜기** (execve) | 현재 프로세스 공간에 새로운 실행 파일을 올려 실행 | **`59`** | **`221`** |
| **11** | **잠깐대기** (nanosleep) | 지정한 나노초(ns) 만큼 스레드를 대기 상태로 전환 | **`240`** | **`101`** |
| **12** | **시간읽기** (gettimeofday) | 1970년 1월 1일 이후 경과된 초/마이크로초 구하기 | **`116`** | **`169`** (또는 VDSO) |
| **13** | **상태확인** (stat/fstat) | 파일의 크기, 소유주, 권한 등 메타 정보 읽기 | **`338`** | **`80`** |
| **14** | **이름변경** (rename) | 파일이나 디렉토리의 이름을 바꾸거나 이동 | **`128`** | **`38`** |
| **15** | **길막기** (ioctl) | 하드웨어 장치에 특수 명령 및 입출력 제어 전달 | **`54`** | **`29`** |
| **16** | **신호전달** (kill) | 특정 프로세스에 강제 종료 및 인터럽트 신호 발송 | **`37`** | **`129`** |
| **17** | **방장권한** (getuid) | 현재 실행 중인 유저의 고유 ID (UID) 획득 | **`24`** | **`174`** |
| **18** | **위치이동** (lseek) | 파일 안에서 읽고 쓸 위치(포인터 커서) 이동 | **`199`** | **`62`** |
| **19** | **폴더생성** (mkdir) | 새로운 디렉토리 경로 생성 | **`136`** | **`34`** |
| **20** | **폴더삭제** (rmdir) | 비어 있는 디렉토리 경로 삭제 | **`137`** | **`35`** |
| **21** | **동기화** (fsync) | 메모리 버퍼의 파일 데이터를 하드디스크에 강제 저장 | **`95`** | **`82`** |
| **22** | **파이프** (pipe) | 프로세스 간 통신(IPC)용 단방향 데이터 통로 개설 | **`42`** | **`59`** |
| **23** | **신호대기** (rt_sigaction) | 특정 신호(Signal) 발생 시 실행할 핸들러 등록 | **`343`** | **`134`** |
| **24** | **소켓개설** (socket) | 네트워크 통신용 엔드포인트(Socket) 생성 | **`97`** | **`198`** |
| **25** | **전화걸기** (connect) | 생성한 소켓을 원격지 서버 주소에 연결 | **`98`** | **`203`** |
| **26** | **전화대기** (accept) | 클라이언트의 소켓 연결 요청을 수락 | **`30`** | **`202`** |
| **27** | **소켓바인딩** (bind) | 소켓을 특정 IP 주소와 포트 번호에 결합 | **`104`** | **`200`** |
| **28** | **리스닝** (listen) | 소켓을 연결 요청을 받을 수 있는 대기 상태로 전환 | **`106`** | **`201`** |
| **29** | **송신** (sendto) | 소켓을 통해 데이터를 네트워크로 전송 | **`133`** | **`206`** |
| **30** | **수신** (recvfrom) | 소켓을 통해 데이터를 네트워크로부터 수신 | **`29`** | **`207`** |
| **31** | **폴더읽기** (getdents) | 디렉토리 내부의 파일 목록 구조체 읽기 | **`지원안함`** | **`61`** |
| **32** | **호스트명** (gethostname) | 현재 시스템의 네트워킹 컴퓨터 이름 획득 | **`87`** | **`161`** |
| **33** | **권한변경** (chmod) | 파일의 읽기/쓰기/실행 보호 권한 플래그 수정 | **`15`** | **`40`** |
| **34** | **소유주변경** (chown) | 파일의 소유 유저 및 그룹 변경 | **`16`** | **`54`** |
| **35** | **링크생성** (symlink) | 파일에 대한 바로가기(심볼릭 링크) 생성 | **`152`** | **`36`** |
| **36** | **시간설정** (utimes) | 파일의 최종 접근 및 수정 시간 수동 세팅 | **`138`** | **`88`** |
| **37** | **경로변경** (chdir) | 프로세스의 현재 작업 디렉토리(Cwd) 변경 | **`12`** | **`49`** |
| **38** | **현재경로** (getcwd) | 현재 작업 중인 디렉토리의 절대경로 문자열 획득 | **`253`** | **`17`** |
| **39** | **우선순위** (setpriority) | 프로세스의 CPU 스케줄링 우선순위(nice) 지정 | **`96`** | **`140`** |
| **40** | **가짜루트** (chroot) | 프로세스가 바라보는 최상위 루트(`/`) 디렉토리를 변경 | **`61`** | **`51`** |

---

## 🛠️ 훈민정음 어셈블러용 시스템 콜 추상화 매크로 설계 예시

대장님의 어셈블리 코드 가독성을 보장하기 위한 헤더 구조라네.

```assembly
; hunmin_syscall.s (시스템콜 무기고 헤더)

#if defined(__APPLE__)
    ; --- 🍎 맥 OS 용 단축키 ---
    .macro 화면출력
        mov x16, #4
        svc #0
    .endm
    .macro 퇴근종료
        mov x16, #1
        svc #0
    .endm
#else
    ; --- 🐧 리눅스 용 단축키 ---
    .macro 화면출력
        mov x8, #64
        svc #0
    .endm
    .macro 퇴근종료
        mov x8, #93
        svc #0
    .endm
#endif
```
