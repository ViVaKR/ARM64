.ifndef SECTION_MACROS_H
.set    SECTION_MACROS_H, 1


// [ Mach-O (마크오) 파일 포맷의 규칙 ] 안내

// --> 인자가 4개(기본)인 경우
// .section [세그먼트 이름], [섹션 이름], [섹션 타입], [섹션 속성]
// 1. __TEXT (첫 번째: 세그먼트 이름)
//   의미: 메모리에 로드될 때의 대분류(Segment) 영역
//   특징: 읽기 전용(Read-Only) 및 실행 가능(Executable) 한 영역을 뜻함
//         프그래그램이 실행되는 동안 절대로 변하면 안 되는 알맹이들이 들어감
// 2. __text (두 번째: 섹션 이름)
//    의미: 대분류 안의 소분류 (Section) 영역 , 대/소문자 엄격 구별함
//    특징: __TEXT 세그먼트 안의 __text 섹션은 진짜 우리가 짠 기계어 코드
//          CPU 가 실행할 명려어가 위치하는 방.
// 3. regular (세 번째: 섹션 타입)
//    의미 : 이 섹션이 어떤 형태의 데이터를 담고 있는지 나타내는 타입(Type)
//    특징 : 특별한 구조(예: 문자열 테이블, 리터럴 풀 등) 가 없는 일반적인 코드나 데이터 의미
// 4. pure_instructions (네 번째: 섹션 속성)
//    의미: 이 섹션의 추가적인 특성(Attribue)을 나타냄.
//    특징: 이 방에는 오직 기계어 명령어(Instruction)만 들어있다는 것을
//          OS(커널)와 CPU에게 확정 지어 알려 주는것으로 여기에 데이터가 섞여 있으면
//          보안상(배후 공격 등) 위험하므로 딱 못 박아둠.

// --> 인자가 2개인 경우
// .section [세그먼트 이름], [섹션 이름]
// [타입] 과 [속성] 은 기본값이거나 필요 없을 때 생략할 수 있음.
// .section __DATA, __data
//     해석 : _DATA 세그먼트의 __data 섹션(읽고 쓰기가 가능한 일반 데이터 방)
//     타입과 속성 생략 : 아 그냥 평범한 수정 가능한 데이터구나. 하고 알아 들으면 됨.

// --> 인자가 3개인 경우
// .section [세그먼트 이름], [섹션 이름], [섹션 타입]
// .section __TEXT, __cstring, cstring_literals
//    해석 : __TEXT 세그먼트의 __cstring 섹션이며, cstring_literals C 스타일 문자열
//           문자열이므로 pure_instructions 같은 속성은 필요 없으므로 3개

// 즉, "첫 두 개(대문자, 소문자)는 주소록의 '시, 구(또는 동)' 주소이고, 뒤에 붙는 단어들은 그 방의 '사용 용도'를 설명하는 설명서다!"

// "애플 기계(Mach-O)는 워낙 깐깐해서, 메모리 방을 만들 때 이름만 주는 게 아니라 '여기선 코드만 실행할 거야!', '여긴 문자열만 둘 거야!'라고 용도를 공증받아야 해서 콤마가 늘어나는 것"

// ----------------------------------------------------------
// [코드 구역] __TEXT,__text
// 명령어는 무조건 4바이트 (2^2) 정렬
// 실제 기계어 명령어가 위치. pure_instructions = 순수 명령어 구역
// ----------------------------------------------------------
.macro CODE_SECTION
    .section __TEXT, __text, regular, pure_instructions
    .align 2
.endmacro

// --------------------------------------------
// [C-스타일 문자열 리터럴]
// printf 포맷 스트링 등 널 종료(null-terminated)
// 문자열 상수 전용(읽기 전용)
// 정렬 없음 : 바이트 스트림
// --------------------------------------------
.macro   CSTRING_SECTION
    .section __TEXT, __cstring, cstring_literals
    .align 3 // Read-Only 문자열 캐시라인 히트율 극대화
.endmacro

// -------------------------------------------------------------
// [읽기전용 상수 데이터]
// 룩업 테이블이나. 배열을 담으므로 8바이트(2^3) 고정이 안전함
// 문자열이 아닌 일반 상수 (룩업 테이블, 상수 배열 등) - 재배피 불필요
// -------------------------------------------------------------
.macro CONST_SECTION
    .section __TEXT, __const
    .align 3
.endmacro

// ----------------------------------------------------
// [4바이트 실수 상수]
// float(32비트) 리터럴 전용 이므로 무조건 4바이트(2^2) 정렬 고정
// ----------------------------------------------------
.macro   LITERAL4_SECTION
    .section __TEXT, __literal4, 4byte_literals
    .align 2
.endmacro

// ---------------------------------------------------
// [8바이트 실수 상수]
// double(64비트) 리터럴 전용이므로 무조건 8바이트(2^3) 정렬 고정
// ---------------------------------------------------
.macro   LITERAL8_SECTION
    .section __TEXT, __literal8, 8byte_literals
    .align 3
.endmacro

// ------------------------------------------------------------
// [초기화된 변수]
// 선언과 동시에 값이 존재하며, 실행 중 수정 가능한 전역/정적 변수 영역
// 일반 전역 변수 구역 안전하게 8바이트 정렬
// ------------------------------------------------------------
.macro DATA_SECTION
    .section __DATA, __data
    .align 3
.endmacro

// -----------------------------------------------------------
// [읽기전용 변수/포인터 테이블]
// 동적 링킹 시점에 주소가 확정된 후 읽기 전용으로 보호되는 변수 구역
// 주소 재배치 후 읽기 전용으로 보호되는 섹션
// 함수 포인터 테이블, 델리게이트 리스트 등에 최적
// 64비트 주소값(.quad)들의 배열이므로 무조건 8바이트(2^3) 정렬
// -----------------------------------------------------------
.macro CONST_DATA_SECTION
    .section __DATA, __const
    .align 3
.endmacro

// ----------------------------------------
// [0으로 초기화된 변수]
// 초기값이 없거나 0인 변수.
// 바이너리 용량을 차지하지 않고 실행 시 0 할당
// 안전하게 8바이트 정렬
// ----------------------------------------
.macro   BSS_SECTION
    .section __DATA, __bss
    .align 3
.endmacro

// -------------------------------------------------------------------
// [공용/잠정 정의 심볼]
// 여러 오브젝트 간 중복 선언된 전역 변수를 링커가 단일화해주는 구역
// .globl _shared_value  ; 1. _sharted_value 라는 이름으로 외부에 공개할 건데,
// .comm _shared_value, 4, 2 ; 4바이트 짜리 임시 공용 예약 공간
// -------------------------------------------------------------------
.macro   COMMON_SECTION
    .section __DATA, __common
.endmacro

.endif
